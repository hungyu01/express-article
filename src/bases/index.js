// Base classes index file
const BaseModel = require('./BaseModel');
const BaseController = require('./BaseController');
const BaseValidator = require('./BaseValidator');

module.exports = {
    BaseModel,
    BaseController,
    BaseValidator
}; 